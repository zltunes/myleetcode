#include <iostream>
#include <string>
using namespace std;

class Solution {
public:
    int atoi(const char *str) {
        if(str == NULL) return 0;
        int cnt = 0;
        while()
        while
            
        }

    }
    bool isLegal(char chr) {
        if(chr >= '0' && chr <= '9') return true;
        return false;
    }
};

int main() {
    return 0;
}
