#include <iostream>
#include <string>
using namespace std;

class Solution {
public:
    int atoi(const char *str) {
        if(str == NULL) return 0;
        char *it = str;
        while(it) {
            if(it == ' ') ++it;
        }
        if(*it >= '0' && *it < '9') {
            
        }

    }
    bool isLegal(char chr)
};

int main() {
    return 0;
}
